[Unit]
Description=lorem
After=network.target
Requires=network.target

#Popub
[Service]
ExecStart=/path/to/popub-relay :PORT :PORT PASSWORD
ExecStop=/bin/kill $MAINPID
ExecReload=/bin/kill -HUP $MAINPID
Type=simple
Restart=on-failure

[Install]
WantedBy=multi-user.target
